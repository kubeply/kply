# Kply vX.Y.Z

## Summary

- One sentence on why this release matters.
- One sentence on who should upgrade.

## Added

- New user-visible commands, flags, output fields, checks, routing adapters, or
  release artifacts.

## Changed

- Behavior changes, output wording changes, policy default changes, or workflow
  changes.

## Fixed

- Bugs fixed in CLI behavior, Kubernetes discovery, session planning, routing,
  reports, docs, CI, or release packaging.

## Upgrade Notes

- Required operator action before or after upgrading.
- Compatibility notes for JSON output, flags, config schema, RBAC, or generated
  Kubernetes resources.

## Verification

- `cargo fmt --all -- --check`
- `cargo check --all-targets --all-features --locked`
- `cargo clippy --all-targets --all-features --locked -- -D warnings`
- `cargo test --all-targets --all-features --locked`
- `cargo deny check`
- `cargo xtask check-release-planning`

## Artifacts

- Binary archives are generated by `cargo-dist`.
- Shell installer is generated by `cargo-dist`.
- SHA-256 checksums and GitHub artifact attestations are generated by the release
  workflow.
